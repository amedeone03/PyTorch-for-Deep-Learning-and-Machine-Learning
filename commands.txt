sudo apt-get install scons libboost-all-dev libprotobuf-dev protobuf-compiler
sudo apt-get install libopenblas-dev libhdf5-serial-dev

scons

sudo cp build/libarmnnDelegate.so /usr/lib/


sudo cp build/libarmnnDelegate.so /usr/lib/

ubuntu@mtk-genio:/usr/lib$ sudo cp build/libarmnnDelegate.so /usr/lib/
cp: cannot stat 'build/libarmnnDelegate.so': No such file or directory

sudo apt-get update
sudo apt-get install scons cmake git wget unzip
sudo apt-get install libboost-all-dev libprotobuf-dev protobuf-compiler
sudo apt-get install libopenblas-dev libhdf5-serial-dev libssl-dev
sudo apt-get install python3-dev python3-pip

git clone https://github.com/tensorflow/tensorflow.git
cd tensorflow
git checkout r2.4  # Choose the version you're working with

git clone https://github.com/google/flatbuffers.git
cd flatbuffers
cmake -G "Unix Makefiles"
make
sudo make install

cd armnn  # Go back to the ARM NN directory
mkdir build
cd build
cmake .. -DARMCOMPUTE_ROOT=/path/to/ComputeLibrary \
         -DARMCOMPUTE_BUILD_DIR=/path/to/ComputeLibrary/build \
         -DBOOST_ROOT=/path/to/boost \
         -DPROTOBUF_ROOT=/path/to/protobuf \
         -DFLATBUFFERS_ROOT=/path/to/flatbuffers \
         -DBUILD_TF_LITE_DELEGATE=1
